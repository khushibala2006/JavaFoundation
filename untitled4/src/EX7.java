import java.util.Scanner;

public class EX7 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int x = 10;

        if ( x > 15 || x % 2 == 0 )
        {
            x = 90;
        }


        System.out.println(x);
  // it descibe that the input is worked in if condition and condition is x is greater than 15 or x modulo 2
   // is always equals to zero , firstly it works on the first condition and if it is not satisfied then the
    // second condition will apply and it gives output

    }
}
